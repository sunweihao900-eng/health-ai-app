page {
  font-family: -apple-system, "PingFang SC", "Helvetica Neue", sans-serif;
  color: #1a2638;
  background: #edf1f7;
  box-sizing: border-box;
}
button { padding: 0; margin: 0; background: none; border: none; line-height: normal; }
button::after { border: none; }
